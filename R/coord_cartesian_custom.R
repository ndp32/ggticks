draw_axis <- function(line_sides,
                      tick_sides,
                      text_sides,
                      breaks,
                      minor_breaks,
                      minor_tick_length,
                      labels,
                      axis_position,
                      panel_params,
                      theme) {

  if (length(breaks) == 0) return(zeroGrob())
}
